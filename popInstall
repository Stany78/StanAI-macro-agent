#!/usr/bin/env bash
set -e

echo "[postInstall] Python:"
python --version

echo "[postInstall] Install Playwright browsers (chromium)…"
python -m playwright install chromium

echo "[postInstall] Done."
